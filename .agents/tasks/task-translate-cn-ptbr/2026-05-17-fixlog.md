# Fix log for review v1 (2026-05-17-023444)

Source: `.agents/tasks/task-translate-cn-ptbr/2026-05-17-023444-review.md`.
The reviewer documented 4 issues. Each is listed below with the action
taken and the lines / files touched.

## Issue 1: Rule D violations - shell comments translated inside fenced code blocks (FIXED)

The CN -> PT-BR pass had translated shell-comment lines and one bullet
list that live *inside* fenced code blocks. Per rule D, fenced content
is verbatim. Reverted the in-fence content to the original Chinese
recovered from `git show main:<path>`.

- `docs/plans/2026-02-18-hook-governance.md` (10 hits): all 10
  `# Validar ...` lines reverted to `# 验证 ...` at lines 67, 86, 107,
  121, 176, 203, 224, 260, 320, 334.
- `docs/plans/2026-02-15-workspace-boundary-guard.md` (7 hits): all 7
  `# Esperado ...` lines reverted to `# 预期 ...` at lines 149, 155,
  161, 167, 173, 179, 185.
- `docs/plans/2026-02-14-v3-cleanup-and-hardening.md` (2 hits):
  `# Devem disparar` -> `# 应触发` at line 93 and
  `# Nao devem disparar` -> `# 不应触发` at line 99.
- `docs/plans/2026-02-14-framework-v3-deterministic-overhaul.md` (5
  hits): the 12-line numbered procedure inside the unlabelled fence at
  lines 240-252 reverted to the original Chinese; the 3-step `git stash`
  comment block at lines 609-619 reverted; the `5. 检查工作流状态` block
  at lines 682-685 reverted; the `agents/*.md` bullet at line 722
  reverted; the `# 新增 file_mtime()` comment at line 733 reverted.
- `docs/plans/2026-02-15-subagent-architecture-optimization.md` (1
  block, 8 lines): the whole bullet list at lines 84-92
  (`Dois subagents: reviewer (review) e researcher (...)`, etc.)
  reverted to the original Chinese (`两个 subagent：reviewer（review）`,
  etc.).
- `skills/omk-debugging/investigation-template.md` (3 placeholder
  labels): the ASCII tree at lines 21-23 reverted from
  `{problema}`/`{direcao de investigacao 1|2}` to `{问题}`/
  `{排查方向 1|2}`.

## Issue 2: Consecrated English tech terms translated to Portuguese (FIXED)

EN-pass `translation-rules.md` section 7 mandates that consecrated tech
terms (`merge`, `branch`, `review`, `build`, `rebuild`, etc.) remain in
English even inside Portuguese sentences. Restored each occurrence
flagged by the review.

- `merge` (mesclar / fusao -> merge):
  - `docs/plans/2026-02-15-reduce-context-bloat.md`: lines 3, 5, 11, 13,
    28, 30 (the H1 already kept `Merge`; body now consistent).
  - `docs/plans/2026-02-14-framework-v3-deterministic-overhaul.md`:
    `### Tarefa 3.1: mesclar a planning skill` -> `### Tarefa 3.1: merge
    da planning skill`; `### Tarefa 3.2: mesclar a reviewing skill` ->
    `### Tarefa 3.2: merge da reviewing skill`; both `**Origens da
    fusao:**` -> `**Origens do merge:**`; the table column header
    `Mesclar em` -> `Merge em`.
  - `docs/plans/2026-02-21-ralph-loop-efficiency.md`: line 5
    (`mesclar funcoes de prompt redundantes` -> `merge de funcoes ...`)
    and line 80 (`### Tarefa 3: mesclar build_prompt e
    build_init_prompt` -> `### Tarefa 3: merge de build_prompt e
    build_init_prompt`).
  - `docs/designs/2026-02-13-framework-v2-upgrade.md`: line 1243 (table
    cell `Remover ou mesclar` -> `Remover ou merge`); line 1744
    (`Mesclar a logica central de verification-before-completion`
    -> `Merge da logica central ...`).
- `branch` (ramo -> branch):
  - `docs/plans/2026-02-16-block-recovery.md` line 91: `ramo fs_write
    usa $FILE, ramo bash usa $CMD` -> `branch fs_write usa $FILE, branch
    bash usa $CMD`.
  - `docs/plans/2026-02-19-ralph-loop-context-optimization.md` line 223:
    `ramo paralelo de build_batch_prompt` -> `branch paralelo de
    build_batch_prompt`.
- `review` (revisao -> review):
  - `docs/plans/2026-02-21-codebase-audit.md` line 1: H1 changed from
    `# Codebase Audit - revisao geral do codigo` to `# Codebase Audit
    - review geral do codigo`.
  - `docs/plans/findings.md` line 126: table row `Revisao multiangulo do
    plano` -> `Review multiangulo do plano`.
  - `docs/designs/2026-02-13-framework-v2-upgrade.md` line 2177: heading
    `apos compensacao, segunda revisao` -> `apos compensacao, segundo
    review`.
- `build` / `rebuild` (construcao / reconstrucao -> build / rebuild):
  - `docs/designs/2026-02-13-framework-v2-upgrade.md` line 1710:
    `### Phase 2: reconstrucao do sistema de Hook` -> `### Phase 2:
    rebuild do sistema de Hook`.
  - `docs/designs/2026-02-13-framework-v2-upgrade.md` line 2323
    (cited): the table cell `construcao via jq -n` was already inside a
    cell that the review counted at row line ~1710 area. The string is
    now `Trocar tudo para build via jq -n`.

## Issue 3: cjk-audit.py under-reported indented fenced blocks (FIXED, defensive)

`.agents/tasks/task-translate-cn-ptbr/cjk-audit.py:21` regex
`^(\`\`\`|~~~)` was tested against `line.strip()`, which already removes
leading whitespace, so functionally the in-fence flag toggled correctly
on indented fences in the verified inputs. Per the reviewer's request
and as a defensive fix in case the input ever changes shape, the regex
is now `^\s*(\`\`\`|~~~)` so it is robust whether the script consumes
`stripped` or the raw `line`.

After the fix and the rule D restorations from issue 1, the audit run
counts grew because the four plan files plus the investigation template
now have CJK back inside their fences:

- Total CJK files: 33 -> 37
- Prose-CJK files outside carve-out: 0 (unchanged)
- Carve-out prose-CJK files: 8 (unchanged)
- In-fence / inline-code-only files: 25 -> 29 (added: `2026-02-14-v3-
  cleanup-and-hardening.md`, `2026-02-15-subagent-architecture-
  optimization.md`, `2026-02-15-workspace-boundary-guard.md`,
  `2026-02-18-hook-governance.md`, `skills/omk-debugging/investigation-
  template.md`)

`audit.md` was updated with the new totals and the expanded in-fence
list, and now mentions the post-review v1 refresh date.

## Issue 4: Awkward Portuguese in episode summary (FIXED, cosmetic)

`knowledge/episodes.md` line 35: `heartbeat: melhor mudar para os 60s
anteriores` rephrased to `heartbeat: voltar para os 60s de antes` per
the natural-rendering options in the review.

The reviewer also cited
`knowledge/archive/episodes-2026-03.md` for the same string, but the
canonical text only lives in `knowledge/episodes.md` (the archive file
contains the three other 2026-03-04 entries, not the heartbeat one),
so no edit was needed there.

## Verification

After the fix pass:

- `python3 -m pytest -q --ignore=...` -> 60 passed, 8 failed
  (matches baseline; pre-existing failures unchanged).
- `bash tests/test-agents-template.sh` -> 27 passed, 1 failed
  (matches baseline; pre-existing `OMK SECTIONS` mismatch on
  `templates/agents-types/coding.md`).
- `bash tests/knowledge/run.sh` -> 1 pass / 3 total scripts
  (matches baseline; pre-existing `l1-rules-injection` and
  `l1-corruption-recall` failures driven by context-enrichment dedup).
- `python3 .agents/tasks/task-translate-cn-ptbr/cjk-audit.py` ->
  0 prose-CJK files outside carve-out, 8 carve-outs, 29 in-fence-only.
- `git diff` shows only `.md` and the audit `.py` script touched; no
  em dashes were introduced.
