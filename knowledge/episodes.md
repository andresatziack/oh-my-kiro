# Episodes (Memória Episódica)

> Eventos com timestamp. ≤30 entradas. Capturados automaticamente por hook + manualmente via @reflect.

<!-- FORMAT: DATE | STATUS | KEYWORDS | SUMMARY -->
<!-- STATUS: active / resolved / promoted -->
<!-- Promotion candidates are computed at runtime (keyword freq ≥3), not stored -->

2026-02-24 | active | research,knowledge,kiro,context,hook | Ao pesquisar o issue de Kiro hook output causando falha de auto-compact: (1) pulou a knowledge base e foi direto para busca externa (o context-enrichment ja sugeriu related episodes mas nao foram lidos); (2) desvio nas palavras-chave (procurou por auto-summary, mas o issue real usa auto-compact); (3) depois de encontrar referencia errada para #1531, parou sem rastrear o numero correto. Issue correto: kirodotdev/Kiro#5792+#5527. Correcao: o fluxo de pesquisa deve primeiro consultar a knowledge base (INDEX.md→episodes→docs) e depois buscar externamente; ao detectar referencia errada e obrigatorio rastrear a fonte correta sem parar
2026-02-15 | active | symlink,fs_read,directory | fs_read Directory mode nao suporta diretorios symlink, use ls ou execute_bash como alternativa

2026-02-19 | active | ralph-loop,verify,plan,format | O comando Verify do plan precisa usar formato inline backtick (**Verify:** `cmd`), nao fenced code block (```bash). O regex de ralph_loop.py so casa inline backtick; um fenced block deixa o parsing incompleto -> erro de sintaxe shell -> 3 rodadas sem progresso disparam o circuit breaker. DO: **Verify:** `cmd`. DON'T: depois de **Verify:** quebrar linha e escrever bloco ```bash





2026-02-20 | active | fs_write,kiro,tool,revert,git-commit | A tool fs_write do Kiro restaura o arquivo modificado ao estado original entre duas tool calls (provavel mecanismo de sandbox no nivel da tool). Sintoma: str_replace/append reportam sucesso mas no proximo tool call o conteudo do arquivo ja regrediu. Solucao: toda modificacao de arquivo precisa ser feita em uma unica chamada execute_bash (use script Python para alterar em lote) e persistida com git commit na mesma chamada. DO: faca modify+verify+commit dentro de um unico comando bash. DON'T: usar fs_write para alterar source code esperando que o proximo tool call enxergue a mudanca

2026-02-20 | active | debugging,lsp,research,skill,industry | A causa raiz da capacidade fraca de debugging do agent nao e "fluxo de boas praticas" insuficiente, e sim a debugging skill ensinar so filosofia e nao ferramentas. Pesquisa de industria mostrou: (1) os SOTA agents (Refact.ai 70.4% SWE-bench) usam todos ferramentas de navegacao semantica (search_symbol_definition/usages), nao dependem de grep; (2) LSP findReferences devolve 23 pontos exatos de chamada vs grep com 500+ de ruido (4x menos token); (3) Refact.ai obriga ao menos 1 chamada de subagent debug_script(), e quando nao se encapsula em tool dedicada o modelo pula o debug e ja altera o codigo; (4) paper SWE-Exp: recuperar 1 experiencia historica funciona melhor, mais que isso piora a performance; (5) tres regras de ouro de lsp-tools: sem goToDefinition nao altere codigo / sem findReferences nao refatore / sem getDiagnostics nao afirme que esta correto. Direcao da solucao: reescrever a debugging skill embutindo a tool chain do LSP + evidencia obrigatoria de diagnostics + matriz de decisao de tools + retrieval em episodes + comparacao de diagnostics antes/depois da mudanca

2026-02-21 | active | phase0,alignment,user-intent,language | A analise de Phase 0 saiu desviada da necessidade do usuario. Dois problemas independentes: (1) o usuario perguntou em chines mas a resposta foi em ingles, faltou o basico de combinar o estilo da entrada; (2) o usuario falou "tempo de execucao/eficiencia" mas o foco foi limpeza de codigo (DRY/fd ambos desligados); depois de ler o codigo o agent ficou preso em detalhes e nao reancorou no requisito original. As causas raiz sao diferentes e nao devem ser misturadas. Correcao: antes de cada achado de Phase 0 sair, alinhe explicitamente com a fala original do usuario, "este achado responde a qual frase do usuario?", e quem nao casar baixa de prioridade ou e descartado
| 2026-02-21 | plan execution bypassed ralph loop | depois do agent terminar o review do plan, ele executou manualmente os 7 tasks em vez de iniciar o ralph loop. O hook enforce-ralph-loop usa modo denylist para detectar pattern de escrita shell (>, sed -i etc.), mas python3 pathlib.write_text() escapou da deteccao. Causa raiz: o hook nao distingue comando de diagnostico de execucao de plan task, e a camada de decisao do agent nao tem aviso forte. Correcao: injetar aviso de ralph loop no session-init | workflow, ralph-loop | active |

### 2026-02-23: sync-omcc.sh sem o passo do symlink agents/
- **Cenario**: depois do gtm integrar OMCC, o Kiro CLI sobe com erro `File URI not found: file://../../agents/reviewer-prompt.md`
- **Causa raiz**: generate_configs.py escreveu `"prompt": "file://../../agents/reviewer-prompt.md"` em reviewer.json/researcher.json, mas sync-omcc.sh nao criou o symlink `agents/` -> `.omcc/agents/`
- **Correcao**: manualmente `ln -sf .omcc/agents agents`
- **Pendente**: sync-omcc.sh deve adicionar Step 3.x para garantir o symlink `agents/`, similar ao tratamento de commands/ e scripts/

2026-03-04 | active | azure-openai,api-key,embedding,openviking | A AZURE_OPENAI_API_KEY do usuario e compativel com o SDK padrao da OpenAI. Uso: client=OpenAI(base_url="https://o3-use.openai.azure.com/openai/v1/", api_key=key). deployment_name="text-embedding-3-large". A variavel de ambiente esta exportada como AZURE_OPENAI_API_KEY no ~/.zshrc. Ao testar o openviking, configure OPENVIKING_EMBEDDING_DENSE_PROVIDER=openai mais o base_url e api_key correspondentes

2026-03-04 | active | timeout,bash,hang,install,openviking | Rodar o script bash de instalacao (ov-install.sh) sem timeout travou e foi interrompido pelo usuario. Dois problemas: (1) qualquer comando que possa ficar interativo ou travar precisa de timeout (no macOS use perl -e 'alarm(N); exec @ARGV'); (2) sai dando comando antes de pesquisar e desperdicou muito tempo tentando coisas no problema de incompatibilidade do binario agfs-server. Caminho correto: pesquisar primeiro a forma certa de instalar o openviking em macOS arm64 (o usuario disse que outra maquina ja foi implantada com sucesso) e so depois agir
2026-03-04 | active | openviking,install,deploy,macos-arm,config,azure | Conhecimento de deploy do OpenViking 0.1.12 em Mac ARM: (1) basta pip install openviking, nao existe CLI ov, tudo via Python/HTTP API; (2) variaveis de ambiente do Azure embedding precisam conter DENSE (OPENVIKING_EMBEDDING_DENSE_*), URL terminando em /openai/v1/, DIMENSION obrigatoriamente em 3072; (3) a inicializacao do SyncOpenViking exige o parametro config (StorageConfig+AGFSConfig backend=local); caso contrario tenta conectar no VikingDB localhost:8080 e da erro; (4) caminho do agfs-server: python3 -c "import openviking,os;print(os.path.join(os.path.dirname(openviking.__file__),'bin','agfs-server'))"; (5) o ov-daemon se comunica via /tmp/omcc-ov.sock; (6) limitacoes conhecidas: o timeout do AGFS e hardcoded em 5s e precisa de 30s+, a compatibilidade com Azure nao e perfeita e exige 0.1.13+; o projeto contorna isso com env e daemon customizado, e e usavel em producao
2026-03-04 | active | heartbeat [correction] | heartbeat: voltar para os 60s de antes 
