2026-02-13 | resolved | skill-chain,skip | Pulou skill-chain e foi direto codificar [hook: enforce-skill-chain]
2026-02-16 | resolved | subagent,reviewer,dispatch,bug | ~~Atribuicao de causa errada: achou que omitir agent_name procurava um kiro_default inexistente~~ Causa real: a allowlist availableAgents so tem ["researcher","reviewer"], o subagent default embutido foi barrado pela allowlist. A doc oficial do Kiro confirma que omitir agent_name usa o subagent default embutido (kiro.dev/docs/cli/chat/subagents). O reviewer dispatch ainda precisa de agent_name:"reviewer"
2026-02-19 | promoted | research,socratic,depth,compaction | Ao pesquisar problemas complexos (otimizacao de long-running agent), pulou o auto-check socratico e jogou direto 6 direcoes de otimizacao. Causa raiz: depois da pesquisa, entrou em modo "martelo procurando prego" - viu o paper dizer que X e problema e assumiu que o framework tambem tem X, sem verificar primeiro se a solucao atual ja cobria (o reinicio de iteration do Ralph Loop = compaction mais forte, foi confundido como "ausente"). Correcao no mecanismo: cada "sugestao/gap" que sai de uma pesquisa precisa passar pelas 3 camadas socraticas antes de entrar em findings: (1) esse problema realmente existe no framework atual (verificar a solucao existente) (2) e viavel na plataforma alvo (restricoes Kiro/CC) (3) o ganho > custo de manutencao. Gatilho: "saida de conclusao de pesquisa" ja e um ponto de decisao chave, nao apenas "escolha de design/solucao"
2026-02-19 | promoted | principle,reform,courage | Ao desenhar a solucao, escolheu uma solucao porca (como progress.md ler so as ultimas 50 linhas em vez de isolar por plan) por achar a "mudanca grande". Correcao do usuario: resultado em primeiro lugar, qualidade em primeiro lugar, sem medo de mudanca grande nem de derrubar fluxo antigo. Novo principio "Bold reform over timid patches" a entrar nos Principles do AGENTS.md
2026-02-13 | promoted | kiro,subagent,hooks | Subagents do Kiro executam hooks customizados por completo (confirmado em teste)
2026-02-13 | promoted | kiro,shell,semantic | Hook do Kiro so suporta command, da para chamar API de LLM via curl para fazer julgamento semantico
2026-02-13 | promoted | refactor,capability | Refatoracao com foco excessivo em features novas quase perdeu a capacidade central do framework antigo
2026-02-13 | promoted | nonfunctional,context,recovery | Falta de design para desafios de execucao prolongada: estouro de contexto / recuperacao de interrupcao / parada precoce
2026-02-13 | promoted | source-file,shell | is_source_file deixou de fora .sh/.yaml/.toml/.tf etc.
2026-02-14 | promoted | context-enrichment,soft-prompt | Lembretes soft foram ignorados x2, precisa virar MANDATORY
2026-02-14 | promoted | reviewer,skip,plan | Plan escrito e reviewer pulado, sem hook = pulado
2026-02-14 | promoted | kiro,plan,builtin | O /plan embutido do Kiro e caixa-preta, nao segue o fluxo customizado; substitua por @plan
2026-02-15 | promoted | subagent,delegation,context | Delegacao seletiva de subagent: capacidade nao degrada / resultado autocontido / tarefa independente; nao delegue tarefas que precisam de code/grep/web tools
2026-02-15 | promoted | kiro,subagent,tools,boundary | Limite rigido de tools de subagent do Kiro (confirmado pela doc oficial): disponiveis = read/write/shell/MCP; indisponiveis = web_search/web_fetch/introspect/thinking/todo_list/use_aws/grep/glob/code. Configurar nao adianta, em runtime fica indisponivel mesmo. resources (file://+skill://) sao carregados no contexto na hora do spawn do agent e nao sao afetados pelos limites de tools. Fonte: kiro.dev/docs/cli/chat/subagents + kiro.dev/docs/cli/custom-agents/configuration-reference
2026-02-15 | promoted | kiro,subagent,mcp,capability | Solucoes MCP para suprir capacidades: ripgrep (substitui grep), filesystem (substitui glob), brave-search (substitui web_search), fetch (substitui web_fetch). A tool code (LSP) nao tem substituto MCP. Validado: no spawn do subagent o mcpServers sobe correto, e as tools MCP do ripgrep funcionam por completo (search/advanced-search/count-matches/list-files etc.). Edicao do JSON do agent surte efeito imediato, mas a allowlist availableAgents do default.json e por sessao
2026-02-15 | promoted | kiro,subagent,architecture | Otimizacao de 4 agents -> 2 agents: removidos implementer/debugger (processo independente do ralph-loop e mais forte), mantidos reviewer + researcher (com MCP suprindo capacidades). No mcp.json do workspace adicione ripgrep; no researcher adicione fetch MCP
2026-02-15 | promoted | kiro,mcp,includeMcpJson | O mcp.json do workspace nao e herdado automaticamente pelo subagent! Precisa configurar `includeMcpJson: true` no JSON do agent para herdar os MCP servers do mcp.json do workspace e do global (default false). Fonte: kiro.dev/docs/cli/custom-agents/configuration-reference + kiro.dev/docs/cli/mcp
2026-02-15 | promoted | fetch,mcp,socksio,proxy | O fetch MCP (mcp-server-fetch) precisa do pacote socksio em ambiente com proxy SOCKS. Correcao: trocar uvx args para ["--with", "socksio", "mcp-server-fetch"]. O processo do MCP server nao reinicia sozinho dentro da sessao, mudancas de configuracao so valem em uma nova sessao
2026-02-15 | promoted | security,workspace,hook | Protecao da fronteira do workspace: block-outside-workspace.sh barra escrita de arquivos fora do workspace. fs_write usa realpath + python3 normpath para checar caminho; bash usa regex para detectar padroes de escrita externa (>/>>redirecionamento/tee/cp/mv/tar -C). git root com fallback PWD, fail-closed. Plugado em todos os agents. Hook a nivel de aplicacao so previne erro de operacao; ataques a nivel de OS exigem sandbox seatbelt/docker
2026-02-15 | promoted | knowledge-v2,rules,memory | Knowledge base v2: rules.md vira estrutura de section por keyword (## [kw1,kw2,...]); o context-enrichment injeta sob demanda a section conforme a keyword da mensagem; episodes promotidos sao limpos automaticamente (mecanismo de esquecimento). Clusterizacao decidida semanticamente pelo agent no self-reflect; sections emergem naturalmente, sem pre-definir
2026-02-15 | promoted | tdd-checklist,verify,hook,plan | Implementacao do enforcement do TDD checklist: 4 camadas de protecao (verificacao da estrutura do plan -> auditoria de cobertura pelo reviewer -> hook na fase de execucao bloqueando check sem evidencia -> stop hook reexecutando verify). Cada item do checklist no formato `- [ ] desc | \`cmd\``; antes de marcar e obrigatorio ter registro de execucao bem-sucedida do cmd (janela de 10 minutos). O exit do pipe while loop se perde na subshell, e obrigatorio usar process substitution `< <(...)`
2026-02-16 | promoted | plan,review,verification | Plan review pulou Round 3: depois de corrigir o feedback do reviewer, o agent decidiu sozinho que "esta ok" e pulou a rodada de validacao. Viola o principio "evidencia -> declaracao". Regra: depois do fix, e obrigatorio re-dispatch do reviewer; so para quando todos APPROVE em uma unica rodada, nao pode autodecidir aprovacao
2026-02-16 | promoted | plan,review,subagent,context | Plan review packet enxuto demais (so header + checklist + 3 frases de resumo) elevou taxa de erro do reviewer: nao via codigo completo da Task / ordem de execucao / marcador Create. Correcao: passe o arquivo de plan completo para o reviewer e evite perder detalhe no processo de resumo
2026-02-18 | promoted | hooks,ws_hash,verify_log | WS_HASH nao mora no common.sh: cada hook define inline o seu `WS_HASH=$(pwd | shasum 2>/dev/null | cut -c1-8 || echo "default")` (SHA-1, nao SHA-256). Arquivos: post-bash.sh:17, pre-write.sh:216. Qualquer script externo que precise ler o verify-log tem de usar o mesmo calculo, senao o caminho nao bate e o log nunca e encontrado
2026-02-18 | promoted | hooks,block-outside-workspace,tmp | block-outside-workspace.sh OUTSIDE_WRITE_PATTERNS na linha 60 tem `'>+\s*/tmp/'`, ou seja, `echo x > /tmp/foo` vai ser barrado. 1 dos 4 reviewers leu errado de novo (perdeu a linha 60) e gerou disputa P1. Licao: quando o review afirmar que o comportamento do codigo contradiz a intuicao, o agent principal deve abrir o arquivo fonte e validar pessoalmente
2026-02-18 | promoted | bash,sed,macos,portability | sed -i do BSD no macOS sem extension suffix da erro (sem editar o arquivo) e gera falso positivo no teste. Para edicao de conteudo JSON cross-platform use `perl -i -pe 's/a/b/'` no lugar de `sed -i`. O pattern do block-sed-json `(sed|awk|perl).*\.json` tambem casa perl, comportamento consistente
2026-02-16 | promoted | config,drift,generator | generate-platform-configs.sh sobrescreve JSON editado a mao quando regenera. enforce-ralph-loop.sh foi registrado a mao no default.json em c902220 e foi sobrescrito pelo config generator do a27860d, sumindo. Licao: todo registro de hook tem de ir para o script generator, nao basta editar o JSON na mao
2026-02-16 | promoted | subagent,availableAgents,whitelist | A allowlist availableAgents limita o escopo de spawn de subagent. Ao omitir agent_name usa-se o subagent default embutido do Kiro, mas se availableAgents nao tiver wildcard nem o default embutido, ele e barrado. Subagents que precisam executar task tem de estar na allowlist
2026-02-16 | promoted | subagent,reviewer,parallel,performance | Plan review deve rodar 1 batch de 4 reviewers em paralelo, nao 2 batches x 2. O mesmo agent_name pode spawnar varias instancias. O limite do use_subagent e ate 4 subagents por chamada, nao 4 agents distintos. Quebrar em batches serializa e desperdica tempo
2026-02-16 | promoted | ralph-loop,stash,data-loss | ralph-loop.sh faz git stash do estado dirty antes de comecar a iter, mas o subagent trabalha em uma instancia nova e nao da pop do stash. Mudancas nao commitadas da sessao atual se perdem. Licao: antes de rodar o ralph-loop, faca commit de tudo
2026-02-16 | promoted | enforce-ralph-loop,absolute-path,hook,bug | A allowlist de fs_write em enforce-ralph-loop.sh usa pattern de caminho relativo (docs/plans/*.md) mas o Kiro entrega caminho absoluto, e o glob de case nao casa, fazendo escritas legitimas serem barradas por engano. Correcao: antes do check da allowlist, use git rev-parse --show-toplevel para tirar o prefixo do workspace e converter em relativo. Ao mesmo tempo descobriu que comandos da allowlist do bash que contem .active tambem sao barrados por engano (grep '.active' casa). Licao: o formato do caminho que o hook recebe precisa ser validado em teste real, nao da para assumir
2026-02-16 | promoted | ralph-loop,orphan,sleep,fd,pipe | A subshell de heartbeat/watchdog do ralph-loop.sh chama sleep direto; ao matar a subshell, o sleep vira orfao (PPID=1) herdando os fds de stdout/stderr e trava permanentemente o caller (execute_bash/kiro-cli) quando o pipe e fechado. Sintoma: ralph-loop reporta SUCCESS mas o caller nao retorna. Correcao: dentro da subshell, rode o sleep em background (`sleep N &; wait $!`) + `trap 'kill %% 2>/dev/null' EXIT`; quando a subshell e morta, o trap dispara e mata o sleep, sem orfao e sem vazamento de fd. Licao: sleeps longos dentro de subshell bash precisam ser passiveis de cleanup pelo processo pai; sleep direto cria orfao incontrolavel
2026-02-16 | promoted | enforce-ralph-loop,checklist,grep,false-positive | enforce-ralph-loop.sh usa `grep -c '^\- \[ \]'` no plan inteiro para contar items unchecked, mas exemplos de codigo / fixtures de teste embutidos no plan tambem contem o texto `- [ ]`, fazendo um plan ja concluido ser considerado incompleto e bloqueando todos os comandos. Correcao: usar awk para extrair so a ultima section `## Checklist` e depois rodar grep. Licao: arquivo de plan e conteudo misto (markdown + codigo embutido); extracao estruturada precisa limitar a section, nao da para fazer grep no arquivo todo
2026-02-16 | promoted | reviewer,context,calibration,prompt | Reviewer repetiu feedback de baixo valor (rollback/encoding/file existence) que o agent principal rejeitou, gastando token. Causa raiz em 3 camadas: (1) o prompt do reviewer exige "cada Task precisa apontar 2 missed scenarios" + "se faltar, REQUEST CHANGES automatico", incentivando exigencia exagerada; (2) o padrao de calibration mora na planning skill mas o reviewer nao ve (resources nao incluem a planning skill); (3) o packet do plan vai resumido e o reviewer chuta sem contexto. Correcao: reviewer-prompt.md ganha padrao de calibration + remove a clausula de obrigatoriedade de criticar; subagent.md ganha regra para passar caminho e o reviewer ler o arquivo; planning skill atualiza tambem o passo de dispatch.
2026-02-17 | promoted | reviewer,verify,semantics | Reviewer auditou comando de verify so olhando sintaxe sem entender intencao: diff retornar 0 = igual = verificacao passou; o reviewer disse que devia ser `! diff -q` (logica invertida). Correcao: reviewer-prompt ganha section "Verify Command Review" exigindo entender intent primeiro e depois checar correctness, com trace manual do exit code
2026-02-17 | promoted | reviewer,prompt,calibration,research | Reviewer prompt foi reescrito com base em pesquisa: (1) principio core das praticas de eng do Google "favor approving when it improves code health" + prefixo Nit; (2) formato obrigatorio de finding (problem + impact + fix, os 3 elementos, sem faltar nenhum); (3) anchor examples (boa finding vs ma finding em comparativo); (4) severidade so em 3 niveis P0/P1/Nit, e so P0/P1 podem REQUEST CHANGES; (5) verify command review exige entender intent primeiro e depois trace do exit code; (6) removidas frases de devil's advocate / challenge every decision e demais que incentivavam exigencia exagerada
2026-02-17 | promoted | ralph,regression,testing | Ralph tem suite de regressao completa (76 tests): `pytest tests/ralph-loop/ -v`. Quando mexer em codigo relacionado ao ralph (scripts/ralph_loop.py, scripts/lib/plan.py, scripts/lib/scheduler.py, scripts/lib/lock.py), e obrigatorio rodar a regressao. Teste do enforcement hook: `bash tests/ralph-loop/test-enforcement.sh`
2026-02-18 | promoted | reviewer,rubber-stamp,quality,prompt | Plan review Round 1 com qualidade baixa: Verify Correctness deu APPROVE em bloco sem trace item a item; Goal Alignment colou rotulo mecanicamente sem questionar a fundo; Clarity deu APPROVE rapido sem dry-run real. Causa raiz em 3 camadas: (1) reviewer-prompt.md nao tem requisito mandatorio de "mostrar o processo de trabalho" (so menciona format mas nao exige show-your-work); (2) o trecho Mission da dispatch query copia direto a angle description da planning skill, abstrato demais, o reviewer nao sabe o que sair de saida; (3) a mission do angle Completeness nao tem scope guard, e o reviewer trata "o plan nao testa funcao existente" como defeito (fora dos Non-Goals)
2026-02-18 | promoted | reviewer,template,fill-blank,quality | 2 rodadas reais com reviewer subagent: "por favor entregue tabela" continua sendo ignorado (Verify Correctness deu blanket APPROVE em ambas), mas "copie esta tabela e preencha cada cell" funciona (Technical Feasibility e Testability ficaram com qualidade boa). Causa raiz: o contexto do subagent e limitado e nao tem hook que force; so da para confiar na restricao do prompt. Melhoria: a mission de angles fixos vira template pre-preenchido em formato de preencher lacunas, reduzindo a chance de preguica. reviewer-prompt.md ganha regra "Fill the template": se a dispatch query traz template de tabela, e obrigatorio preencher cada cell; pular = review REJECTED
2026-02-18 | promoted | hook,enforce-ralph-loop,ordering,bug | enforce-ralph-loop.sh executa o check de chaining/pipe antes da allowlist read-only, fazendo pipes read-only legitimos (`grep|head`) e redirect de stderr (`unlink 2>/dev/null||true`) serem barrados por engano. Correcao: extraia FIRST_CMD, case primeiro com a allowlist read-only (libera pipe para outros comandos read-only) e depois faca o check de chaining como rede de seguranca. Licao: a ordem dos checks de gate hook importa muito - allowlist primeiro, blocklist depois, senao operacao legitima vai para o caco
2026-02-18 | promoted | cc,headless,auth,testing | `claude -p` (modo headless) precisa de autenticacao previa, nao pode abrir OAuth automaticamente como o modo interativo. Quando `claude auth status` mostra loggedIn:false, `-p` sai com exit 1 direto. Solucao: setar a variavel de ambiente `ANTHROPIC_API_KEY`, ou fazer SSH port forwarding para OAuth. O detect_cli() do plan tem de validar status de auth e, sem auth, fazer fallback para Kiro
2026-02-18 | promoted | cc,macos,timeout,compatibility | macOS nao tem o comando `timeout` (GNU coreutils). Plans escritos com `timeout 60s` no macOS dao command not found. Substitutos: `gtimeout` (brew install coreutils) ou `perl -e 'alarm(N); exec @ARGV'`. Nenhum script bash cross-platform pode supor que timeout exista
2026-02-18 | promoted | cc,allowedTools,subagent,Task | O `--allowedTools` do CC headless precisa incluir explicitamente `Task` para conseguir despachar subagent. O plan inicial so escreveu `Bash,Read,Write,Edit`, e os subagents reviewer/executor nao podiam ser despachados. Tambem precisa de `WebSearch,WebFetch` para o researcher. Licao: allowedTools e allowlist; esqueceu = funcionalidade falta
2026-02-18 | promoted | cc,PermissionRequest,headless | A doc oficial do CC e clara: hooks de `PermissionRequest` nao disparam em modo `-p` (nao interativo). Nossos hooks sao todos PreToolUse, entao nao somos afetados, mas o teste de integracao nao consegue testar comportamento ligado a PermissionRequest
2026-02-18 | promoted | testing,coverage,require-regression | test-kiro-compat.sh deixou de fora o require-regression.sh (ja wired em settings.json e pilot.json). A Task 5 do plan tem de enumerar os hooks wired do settings.json, nao da para apenas espelhar arquivos de teste existentes. Licao: a matriz de cobertura de teste deve ser feita comparando com o config fonte, nao com testes ja existentes
2026-02-18 | promoted | reviewer,source-reading,verdict,quality | Tres problemas de qualidade em uma rodada de Review: (1) Goal Alignment sugeriu trocar exit 2 por exit 0 sem ler o source (mesmo padrao do #32); (2) os 4 reviewers nao deram Verdict claro; (3) qualidade muito desigual entre angles (Completeness ⭐4 vs Goal Alignment ⭐2). Causa raiz: a dispatch query nao tem source reading mandate, a regra de verdict do prompt nao e suficientemente impositiva, a mission de Goal Alignment e abstrata demais. Correcao: dispatch ganha section Mandatory Source Reading; prompt ganha regra mandatory para verdict; Goal Alignment ganha instrucao fill-every-cell
2026-02-19 | promoted | reviewer,subagent,parallel,process | No review subiu so 1 subagent fazendo review completo, e o usuario corrigiu para subir 4 reviewers em paralelo (cada um cuidando de um angle: Goal Alignment / Verify Correctness / Completeness / Technical Feasibility). Causa raiz: nao reli a reviewing skill antes de executar e operei de memoria. Licao: antes de disparar qualquer skill e obrigatorio ler o SKILL.md correspondente, nao da para executar de impressao. O modo paralelo de 4 angles e o fluxo central da reviewing skill; veja o formato da section Review do plan de context-optimization
2026-02-19 | promoted | principle,reform,timid,optimization | Na analise de solucoes de otimizacao, recuou para otimizacoes pequenas (3-9% de ganho) por achar "muitos efeitos colaterais" e "mudanca grande", evitando reforma a nivel de arquitetura (paralelismo multi-processo + isolamento por worktree). Correcao do usuario: a diretriz de topo "Bold reform over timid patches" exige resultado em primeiro lugar, sem medo de trabalhao. Efeito colateral nao e desculpa para evitar, e problema de engenharia para resolver. DO: defina primeiro o objetivo de melhor resultado e depois resolva os efeitos colaterais da implementacao. DON'T: rebaixar o objetivo e escolher solucao porca por causa dos efeitos colaterais
2026-02-19 | promoted | memory,consolidation,episodic,semantic | Consenso da industria: a destilacao automatica de episodic -> semantic e o nucleo do sistema de memoria. LangMem consolida automaticamente a cada conversa (insert/update/delete), SAGE usa a curva de esquecimento de Ebbinghaus para descartar por entropia da informacao, Bedrock AgentCore traz prompt de consolidation de fabrica. O sistema atual tem 47 episodes / 0 promovidos = pipeline quebrado. Fonte: langchain-ai.github.io/langmem, arxiv 2409.00872, aws blog
2026-02-19 | promoted | memory,noise,retrieval,CTIM | Aviso do paper CTIM-Rover: SE agent ganhou episodic memory e a performance caiu (42% -> 31%). Causa raiz: registros de memoria irrelevantes injetam ruido e atrapalham a decisao. Um unico registro irrelevante com a palavra "clean" levou o agent a investigar a funcao errada. Licao: precisao do retrieval > volume de memoria; injecao integral e desastre. Fonte: arxiv 2505.23422
2026-02-19 | promoted | memory,formation,hot-path,background | A formacao de memoria tem dois momentos: hot-path (durante a conversa, em tempo real, efeito imediato porem aumenta latencia) e ideal para correcoes criticas; background (apos a conversa, assincrono, sem afetar a resposta) e ideal para descoberta de padroes e destilacao de regras. Hoje auto-capture = hot-path e session-init = background; a combinacao e razoavel mas falta a destilacao automatica na fase background. Fonte: langchain-ai.github.io/langmem concepts
2026-02-19 | resolved | research,king-mode,gemini,socratic-validation | Pesquisa sobre Gemini King Mode (AICodeKing). 6 mecanismos: zero-fluff/library-discipline/intentional-minimalism/avant-garde-persona/ultrathink-trigger/edge-case-analysis. Apos validacao socratica item a item, conclusao: 0 gaps. Motivo: King Mode no fundo e band-aid em camada de prompt para compensar a preguica padrao do Gemini; nosso framework usa governanca estruturada (hooks+skills+TDD+multi-reviewer) para resolver problemas da mesma classe em camada mais profunda. ultrathink e o mais proximo de algo de valor, mas plataformas CC/Kiro ja suportam nativamente e nos temos Socratic self-check. Fonte: gist.ly/youtube-summarizer/boost-gemini-3-coding-with-king-mode-prompt, recapio.com/digest/gemini-3-pro-ultra-mode-kingmode-prompt
2026-02-19 | resolved | ralph-loop,check-off,parallel,worktree | check_off() supunha mapeamento Task:Checklist = 1:1, mas na pratica pode ser 1:N (6 tasks, 15 items). Correcao: verify_and_check_all() percorre todos os items unchecked, roda o verify command de cada um e marca os que passam, sem depender do mapeamento posicional task -> item
2026-02-19 | resolved | git,active,staged,commit,guard | No git commit, .active staged pode conter caminho de plan antigo (stale staged change), reativando por acidente um plan ja encerrado. Correcao: enforce-ralph-loop.sh ganha early guard que, antes do commit, checa se o .active staged difere do HEAD
2026-02-20 | resolved | test,worktree,merge,pollution,cwd | test_parallel_batch_creates_worktrees rodava ralph_loop.py no repo principal -> criava worktree branch -> merge commit poluia o history do repo principal. Ao mesmo tempo, a fixture git_repo nao restaurava o os.chdir(tmp_path), poluindo o cwd. Correcao: fixture autouse em tests/conftest.py guarda cwd e HEAD juntos, e depois do teste faz reset --hard de volta ao HEAD original e remove branches stale. Licao: se o teste roda subprocess em repo real, e obrigatorio guardar todos os efeitos colaterais (HEAD/branches/worktrees), nao so o cwd
2026-02-20 | resolved | stale-process,kiro-cli,background,merge | Sessoes antigas do kiro-cli (com worker do ralph loop) ficaram rodando em background por dias, fazendo git merge ralph-worker-w2-i1 sem parar. Matar os processos stale resolveu. Licao: depois que uma sessao de agent de longa duracao encerra, verifique se sobrou processo (ps aux | grep kiro-cli)
